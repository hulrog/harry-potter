import classes from "./PostHistory.module.css";

function PostHistory({ userId }) {
  // TODO post history userids

  return (
    <div className={classes.container}>
      <h1>User Profile</h1>
    </div>
  );
}

export default PostHistory;
